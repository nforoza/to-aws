name: CI Workflow
on: [push] # Trigger on any push event
jobs:
  build:
    runs-on: ubuntu-latest # Use a GitHub-hosted Linux runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # Uses a pre-made action to clone your repo
      - name: Run a script
        run: echo "Hello, GitHub Actions!"